#通用信息

spring:
  profiles:
    active: dev # 设置默认激活的环境



#开发环境
server:
  servlet:
    context-path: "/aaa"
---
spring:
  config:
    activate:
      on-profile: dev

server:
  port: 8081
---

#测试环境

spring:
  config:
    activate:
      on-profile: test

server:
  port: 8082

---
#生成环境

spring:
  config:
    activate:
      on-profile: pro

server:
  port: 8083